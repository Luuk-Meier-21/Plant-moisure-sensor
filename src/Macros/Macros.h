#ifndef Utils_h
#define Utils_h

#define LENGTH(a) (*(&a + 1) - a)

// namespace Utils
// {
//   static const size_t length(auto array[])
//   {
//     const size_t a = *(&array + 1) - array;
//     return a;
//   }
// };

#endif
